import { ForzasharePipe } from './forzashare.pipe';

describe('ForzasharePipe', () => {
  it('create an instance', () => {
    const pipe = new ForzasharePipe();
    expect(pipe).toBeTruthy();
  });
});
